/**
 * Aeonic Middleware Index
 * Created by: Kyle Erickson
 * Jan 20, 2017
 */

import { Router } from 'express';

export default ( { config, db } ) => {
    let api = Router();

    //TODO: add middleware

    return api;
};